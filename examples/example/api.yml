
rpc:
  add:
    parameters:
      - name: a
        schema:
          type: integer
      - name: b
        schema:
          type: integer
    response:
      schema: 
        type: integer

http:
  /add:
    get:
      parameters:
        - name: a
          in: query
          schema:
            type: integer
        - name: b
          in: query
          schema:
            type: integer

# http-json-rpc:
#   - new-video

http-auth:
  jwt:
    type: bearer
    bearerFormat: jwt
